"""
settings.py  v4.0
==================
定義ファイル.xlsx に基づくシフト種類と実働時間のマスター定義。
"""

from typing import Dict, List

# 従業員名簿のデフォルト
DEFAULT_ROSTER: List[str] = [
    "末吉 弘一",
    "伊藤 晶俊",
    "吉村 智",
    "南 英俊",
    "杉田 孝行",
    "山田 誠",
    "大西 信一",
    "村主 博",
    "河内 拳",
]

# 固定勤務（シフト自動生成から外して平日日勤のみにする人）
DEFAULT_FIXED_WORKER: str = "末吉 弘一"

# 新しいシフト種類と実働時間 (定義ファイル.xlsx に準拠)
DEFAULT_SHIFT_HOURS: Dict[str, int] = {
    "隊長日勤": 8,   # 末吉さん専用
    "日勤A":   8,   # 通常日勤
    "日勤B":   8,   # 通常日勤
    "A":      9,   # 夜勤A（実働9h）
    "9B":     9,   # 夜勤B（実働9h）
    "C":      9,   # 夜勤C（実働9h）
    "○":      0,   # 公休
    "年休":    8,   # 有給休暇（労働時間としてカウント）
    "△":      0,   # 待機（公休扱い）
    "◎":      0,   # 普段は使用しない休日扱い
}

# 制約パラメータのデフォルト
DEFAULT_CONSTRAINTS: Dict[str, int] = {
    # 月間上限時間は optimizer 側で日数（31日=176h, 30日=168h）に応じて自動でピッタリ一致させます
    "連勤制限":             5,  # 最大連勤日数
}
